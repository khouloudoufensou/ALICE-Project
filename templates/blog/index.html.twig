{% extends 'base.html.twig' %}

{% block title %}Blog de l'association{% endblock %}

{% block body %}

<div class="text-center">
<h1>Welcome sur le blog de notre association</h1>
<div class="lead">Découvrez les secrets de la nature</div>
</div>

    {# <section class="articles">
    {% for article in articles %}
     <div class="container">
            <h2>{{ article.title }}</h2>
        <div class="metadata">Ecrit le {{ article.createdAt | ('d/m/Y')}} </div>
        <div class="content">
        <img src="{{ article.image }}" alt="">
        {{ article.content |raw}}
        <a href="{{ path('blog_article') }}" class="btn btn-secondary mb-2">Lire la suite</a>
        </div>
     </div>
        
    {% endfor %}

    </section> #}



<div class="row mt-5">
{% for article in articles %}
            <div class="col-md-6 col-lg-4 mb-3">
                <div class="card">
                    <a href="{{ path('blog_article', {id: article.id}) }}">
                        <div class="img-ratio" style="--ratio: 60%;">
                            <div class="img">
                                <img src="{{ asset(article.imagePath) }}" class="card-img-top w-100" alt="...">
                            </div>
                        </div>
                    </a>
                    <div class="card-body">
                        <h5 class="card-title">{{ article.title }}</h5>
                        <p class="card-text">{{ article.contentEllipsis }}</p>
                    </div>
                </div>
            </div>
         {% endfor %}
    </div>

    <div class="navigation">
        {{ knp_pagination_render(articles) }}
    </div>


{% endblock %}
